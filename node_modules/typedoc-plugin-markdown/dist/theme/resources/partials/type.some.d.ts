import { MarkdownThemeContext } from '../../index.js';
import { SomeType } from 'typedoc';
/**
 * Takes a generic Type and returns the appropriate partial for it.
 *
 * @category Type Partials
 */
export declare function someType(this: MarkdownThemeContext, model?: SomeType): string;
