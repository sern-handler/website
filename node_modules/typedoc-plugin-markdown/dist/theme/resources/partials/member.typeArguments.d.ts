import { MarkdownThemeContext } from '../../index.js';
import { SomeType } from 'typedoc';
/**
 * @category Member Partials
 */
export declare function typeArguments(this: MarkdownThemeContext, model: SomeType[], options?: {
    foreCollpase?: boolean;
}): string;
