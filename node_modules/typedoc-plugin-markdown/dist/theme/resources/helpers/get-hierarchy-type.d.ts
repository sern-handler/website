import { MarkdownThemeContext } from '../../../public-api';
import { SomeType } from 'typedoc';
export declare function getHierarchyType(this: MarkdownThemeContext, model: SomeType, options?: {
    isTarget: boolean;
}): string;
