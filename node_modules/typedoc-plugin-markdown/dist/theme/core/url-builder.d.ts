import { MarkdownTheme } from '../markdown-theme';
import { UrlMapping } from '../theme-types';
import { ProjectReflection, Reflection } from 'typedoc';
/**
 * Map the models of the given project to the desired output files.
 * Based on TypeDoc DefaultTheme.getUrls()
 *
 * @param project  The project whose urls should be generated.
 */
export declare function buildUrls(theme: MarkdownTheme, project: ProjectReflection): UrlMapping<Reflection>[];
