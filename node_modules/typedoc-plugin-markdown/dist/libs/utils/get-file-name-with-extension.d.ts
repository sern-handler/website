/**
 * Returns a filename with extension while normalizing both the input name and input extension.
 */
export declare function getFileNameWithExtension(fileName: string, fileExtension: string): string;
